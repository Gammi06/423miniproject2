<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject2.domain.wanteds.WantedsDao">
	<!-- 수현 작업 시작 -->

	<insert id="insert">
		INSERT INTO wanteds(title, detail, position_code_id, career_code_id, pay, enddate, created)
		VALUES(#{wantedTitle},#{wantedDetail},#{positionCodeId},#{careerCodeId},#{pay},#{enddate}, NOW())
	</insert>

	<update id="updateById">
		Update wanteds SET title = #{wantedTitle}, detail = #{wantedDetail},
		position_code_id = #{positionCodeId}, career_code_id = #{careerCodeId},
		pay = #{pay}, enddate = #{enddate}
		WHERE id = #{id}
	</update>

	<delete id="deleteById">
		DELETE FROM wanteds WHERE id = #{id}
	</delete>

	<!-- 수현 작업 완료 이 밑으로는 쿼리 안건드렸습니다~!-->

	<select id="findById"
		resultType="site.metacoding.miniproject2.dto.WantedDetailRespDto">
		SELECT w.id, pc.name, cc.name, w.title,
		c.company_name, '지역', IF(w.enddate > NOW(), '모집중', '마감') AS status
		FROM wanteds w
		LEFT OUTER JOIN positions_code pc
		ON pc.id = w.position_code_id
		LEFT OUTER JOIN careers_code cc
		ON cc.id = w.career_code_id
		LEFT OUTER JOIN companys c
		ON c.id = w.company_id
		WHERE w.enddate > NOW()
		AND w.id = #{id}
		ORDER BY w.created DESC
	</select>

	<select id="findAll"
		resultType="site.metacoding.miniproject2.dto.WantedListRespDto">
		SELECT w.id, pc.name, cc.name, w.title,
		c.company_name, '지역', IF(w.enddate > NOW(), '모집중', '마감') AS status
		FROM wanteds w
		LEFT OUTER JOIN positions_code pc
		ON pc.id = w.position_code_id
		LEFT OUTER JOIN careers_code cc
		ON cc.id = w.career_code_id
		LEFT OUTER JOIN companys c
		ON c.id = w.company_id
		WHERE w.enddate > NOW()
		ORDER BY w.created DESC
	</select>

<!--
	구현예정
	<select id="findAllOrdered"
		resultType="site.metacoding.miniproject2.dto.WantedListRespDto">
	</select>
-->

	<select id="findAllByCompanyId"
		resultType="site.metacoding.miniproject2.dto.WantedListRespDto">
		SELECT w.id, pc.name, cc.name, w.title,
		c.company_name, '지역', IF(w.enddate > NOW(), '모집중', '마감') AS status
		FROM wanteds w
		LEFT OUTER JOIN positions_code pc
		ON pc.id = w.position_code_id
		LEFT OUTER JOIN careers_code cc
		ON cc.id = w.career_code_id
		LEFT OUTER JOIN companys c
		ON c.id = w.company_id
		WHERE w.enddate > NOW()
		AND c.id = #{companyId}
		ORDER BY w.created DESC
	</select>

	<select id="findAllByPositionCodeName"
		resultType="site.metacoding.miniproject2.dto.WantedListRespDto">
		SELECT w.id, pc.name, cc.name, w.title,
		c.company_name, '지역', IF(w.enddate > NOW(), '모집중', '마감') AS status
		FROM wanteds w
		LEFT OUTER JOIN positions_code pc
		ON pc.id = w.position_code_id
		LEFT OUTER JOIN careers_code cc
		ON cc.id = w.career_code_id
		LEFT OUTER JOIN companys c
		ON c.id = w.company_id
		WHERE w.enddate < NOW()
		AND pc.name = #{positionCodeName}
		ORDER BY w.created DESC
	</select>

	<select id="findAllByLike"
		resultType="site.metacoding.miniproject2.dto.WantedListRespDto">
		SELECT w.id, pc.name, cc.name, w.title,
		c.company_name, '지역', IF(w.enddate > NOW(), '모집중', '마감') AS status
		FROM wanteds w
		LEFT OUTER JOIN positions_code pc
		ON pc.id = w.position_code_id
		LEFT OUTER JOIN careers_code cc
		ON cc.id = w.career_code_id
		LEFT OUTER JOIN companys c
		ON c.id = w.company_id
		LEFT OUTER JOIN likes lk
		ON lk.wanted_id = w.id
		WHERE w.enddate > NOW()
		AND lk.user_id = #{userId}
		ORDER BY w.created DESC
	</select>

</mapper>