<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.metacoding.miniproject2.domain.resumes.ResumesDao">

	<!-- >>>> 연지 작업함 <<<< -->
	<insert id="insert"></insert>

	<select id="findAll" resultType="site.metacoding.miniproject2.dto.ResumesRespDto.ResumeListRespDto">
		SELECT id, title, created FROM resumes
		WHERE user_id = #{id}
	</select>

	<select id="findById" resultType="site.metacoding.miniproject2.dto.ResumesRespDto.ResumeListRespDto"></select>

	<update id="updateById"></update>

	<delete id="deleteById">
		DELETE FROM resumes WHERE id = #{id}
	</delete>
	<!-- >>>> 연지 작업함 <<<< -->

</mapper>